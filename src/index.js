/* It works with just importing because todoController got already
initialized in todoController.js. Though it might be better practice
to do the initalizing here. */
import { todoController } from './todoController'
